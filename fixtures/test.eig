func createCounter() {
  let c = 0
  ret func() {
    c = c + 1
    ret c
  }
}

let getCount1 = createCounter()
let getCount2 = createCounter()

emitln(getCount1())
emitln(getCount2())
emitln(getCount1())
