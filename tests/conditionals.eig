if true emitln("a") else emitln("b")
if false emitln("a") else emitln("b")

if true {
	emitln("should run 1")

	if false emitln("should not run 2") else {
		emitln("should run 2")
		
		if 1 + 1 ?= 2 emitln("should run 3")
		else emitln("should not run 3")
	}
} else emitln("should not run 1")
